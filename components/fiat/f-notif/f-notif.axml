<import-sjs name="element" from="../../../utils/element.sjs"/>

<view class="{{ element.classAttr('f-notif', className,) }}">
  <label class="{{type}}">
    <view a:if="{{type === 'number'}}">
    {{ amount }}
    </view> 
  </label>
</view>